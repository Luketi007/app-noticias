import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_noticias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
